// Program to print all combination of size r in an array of size n 
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int combinations(arr1, arr2, size){
	int final[];

}

// Driver program to test above functions 
int main() 
{ 
	int arr[] = {1, 2, 3, 4, 5}; 
	int r = 3; 
	int n = sizeof(arr)/sizeof(arr[0]); 
	printCombination(arr, n, r); 
} 
